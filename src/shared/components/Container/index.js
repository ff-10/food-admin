import { ContainerBody } from "./Container.styled";





export default function Container({ children }) {

    return (
        <ContainerBody >
            {children}
        </ContainerBody>
    )

}

